{
  "stype": "fix",
  "label": "FIX",
  "blurb": "Something is broken or wrong — diagnose it and repair it.",
  "form": [
    {
      "key": "what",
      "label": "WHAT BROKE",
      "required": true,
      "multiline": true
    },
    {
      "key": "seen",
      "label": "WHERE SEEN",
      "required": false,
      "multiline": false
    },
    {
      "key": "repro",
      "label": "REPRO STEPS",
      "required": false,
      "multiline": true
    }
  ],
  "stages": [
    {
      "id": "reproduce",
      "label": "REPRODUCE",
      "gate": false,
      "instructions": "Reproduce the reported failure before touching anything. Find the smallest command, request, or test that shows it. If you cannot reproduce it, say so with the evidence you gathered and do not advance. Put the failing output in `evidence` verbatim, with the command that produced it.",
      "card_fields": [
        {
          "name": "reproduced",
          "type": "verdict"
        },
        {
          "name": "evidence",
          "type": "output"
        }
      ],
      "input": "evidence"
    },
    {
      "id": "rootcause",
      "label": "ROOT-CAUSE",
      "gate": true,
      "instructions": "Trace the failure to its cause. Grep every caller of the function you suspect; the fix belongs where all callers route through, not on the one path the report names. State the cause and the fix you propose. Do not edit code in this stage. Lay `cause` out as an ordered chain — symptom, cause, fix, proof — because the order is the argument.",
      "card_fields": [
        {
          "name": "cause",
          "type": "chain"
        },
        "fix_plan"
      ],
      "input": "approve"
    },
    {
      "id": "fix",
      "label": "FIX",
      "gate": false,
      "instructions": "Apply the approved fix. Smallest diff that kills the root cause. List every file you changed. Show the hunk for each file you changed.",
      "card_fields": [
        {
          "name": "changed",
          "type": "diff"
        }
      ]
    },
    {
      "id": "verify",
      "label": "VERIFY",
      "gate": false,
      "instructions": "Prove the fix: rerun the reproduction, run the relevant tests. Report the exact commands and their results. Only claim pass on output you actually saw.",
      "card_fields": [
        {
          "name": "checks",
          "type": "checks"
        },
        {
          "name": "pass",
          "type": "verdict"
        }
      ],
      "next_allowed": [
        "fix"
      ]
    }
  ]
}
